using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.Advertisements;

public class Adv : MonoBehaviour {
/*
    public static GameObject skipMesssage;
    public static GameObject finishMessage;
    public static bool isReady;
    // Use this for initialization
    void Start() {
        Advertisement.Initialize("1468600", true);
        //Find skipmessage and set to false
        //Find FinishMessage and set to false
        StartCoroutine(Ad());
    }
    // Update is called once per frame
    void Update() {
        isReady = !Advertisement.isShowing;
    }
    public static IEnumerator Ad() {
        while (!Advertisement.IsReady())
            yield return new WaitForEndOfFrame();
        ShowOptions options = new ShowOptions { resultCallback = AdResult };
        Advertisement.Show(options);
    }
    public static void AdResult(ShowResult result) {
        if (result == ShowResult.Skipped)
            skipMesssage.SetActive(true);
        if (result == ShowResult.Finished)
            finishMessage.SetActive(false);
    }*/
}

